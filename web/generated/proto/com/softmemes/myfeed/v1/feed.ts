// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: com/softmemes/myfeed/v1/feed.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../../../../google/protobuf/timestamp";
import { PageRequest, PageResponse } from "./common";

export const protobufPackage = "com.softmemes.myfeed.v1";

/**
 * FeedItem is a single piece of content surfaced in the user's feed.
 * It is produced by a Subscription and links back to both the Source and the
 * Subscription that generated it. Consumption happens on the original platform;
 * myfeed surfaces a preview and link only.
 * platform_type mirrors Source.platform_type (e.g. "youtube", "rss").
 */
export interface FeedItem {
  id: string;
  sourceId: string;
  subscriptionId: string;
  platformType: string;
  title: string;
  description: string;
  /** URL to the content on the original platform. */
  url: string;
  imageUrl: string;
  publishedAt?: string | undefined;
}

export interface GetFeedRequest {
  page?:
    | PageRequest
    | undefined;
  /** Filter to a specific source connection. */
  sourceId: string;
  /** Filter to a specific subscription (e.g. one channel). */
  subscriptionId: string;
  /** Return only items published after this timestamp. */
  after?:
    | string
    | undefined;
  /** Return only items published before this timestamp. */
  before?: string | undefined;
}

export interface GetFeedResponse {
  items: FeedItem[];
  page?: PageResponse | undefined;
}

function createBaseFeedItem(): FeedItem {
  return {
    id: "",
    sourceId: "",
    subscriptionId: "",
    platformType: "",
    title: "",
    description: "",
    url: "",
    imageUrl: "",
    publishedAt: undefined,
  };
}

export const FeedItem: MessageFns<FeedItem> = {
  encode(message: FeedItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.sourceId !== "") {
      writer.uint32(18).string(message.sourceId);
    }
    if (message.subscriptionId !== "") {
      writer.uint32(26).string(message.subscriptionId);
    }
    if (message.platformType !== "") {
      writer.uint32(34).string(message.platformType);
    }
    if (message.title !== "") {
      writer.uint32(42).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(50).string(message.description);
    }
    if (message.url !== "") {
      writer.uint32(58).string(message.url);
    }
    if (message.imageUrl !== "") {
      writer.uint32(66).string(message.imageUrl);
    }
    if (message.publishedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.publishedAt), writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FeedItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFeedItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sourceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.subscriptionId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.platformType = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.imageUrl = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.publishedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FeedItem {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      sourceId: isSet(object.sourceId)
        ? globalThis.String(object.sourceId)
        : isSet(object.source_id)
        ? globalThis.String(object.source_id)
        : "",
      subscriptionId: isSet(object.subscriptionId)
        ? globalThis.String(object.subscriptionId)
        : isSet(object.subscription_id)
        ? globalThis.String(object.subscription_id)
        : "",
      platformType: isSet(object.platformType)
        ? globalThis.String(object.platformType)
        : isSet(object.platform_type)
        ? globalThis.String(object.platform_type)
        : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      imageUrl: isSet(object.imageUrl)
        ? globalThis.String(object.imageUrl)
        : isSet(object.image_url)
        ? globalThis.String(object.image_url)
        : "",
      publishedAt: isSet(object.publishedAt)
        ? globalThis.String(object.publishedAt)
        : isSet(object.published_at)
        ? globalThis.String(object.published_at)
        : undefined,
    };
  },

  toJSON(message: FeedItem): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.sourceId !== "") {
      obj.sourceId = message.sourceId;
    }
    if (message.subscriptionId !== "") {
      obj.subscriptionId = message.subscriptionId;
    }
    if (message.platformType !== "") {
      obj.platformType = message.platformType;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.imageUrl !== "") {
      obj.imageUrl = message.imageUrl;
    }
    if (message.publishedAt !== undefined) {
      obj.publishedAt = message.publishedAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FeedItem>, I>>(base?: I): FeedItem {
    return FeedItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FeedItem>, I>>(object: I): FeedItem {
    const message = createBaseFeedItem();
    message.id = object.id ?? "";
    message.sourceId = object.sourceId ?? "";
    message.subscriptionId = object.subscriptionId ?? "";
    message.platformType = object.platformType ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.url = object.url ?? "";
    message.imageUrl = object.imageUrl ?? "";
    message.publishedAt = object.publishedAt ?? undefined;
    return message;
  },
};

function createBaseGetFeedRequest(): GetFeedRequest {
  return { page: undefined, sourceId: "", subscriptionId: "", after: undefined, before: undefined };
}

export const GetFeedRequest: MessageFns<GetFeedRequest> = {
  encode(message: GetFeedRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(10).fork()).join();
    }
    if (message.sourceId !== "") {
      writer.uint32(18).string(message.sourceId);
    }
    if (message.subscriptionId !== "") {
      writer.uint32(26).string(message.subscriptionId);
    }
    if (message.after !== undefined) {
      Timestamp.encode(toTimestamp(message.after), writer.uint32(34).fork()).join();
    }
    if (message.before !== undefined) {
      Timestamp.encode(toTimestamp(message.before), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetFeedRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFeedRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sourceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.subscriptionId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.after = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.before = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFeedRequest {
    return {
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
      sourceId: isSet(object.sourceId)
        ? globalThis.String(object.sourceId)
        : isSet(object.source_id)
        ? globalThis.String(object.source_id)
        : "",
      subscriptionId: isSet(object.subscriptionId)
        ? globalThis.String(object.subscriptionId)
        : isSet(object.subscription_id)
        ? globalThis.String(object.subscription_id)
        : "",
      after: isSet(object.after) ? globalThis.String(object.after) : undefined,
      before: isSet(object.before) ? globalThis.String(object.before) : undefined,
    };
  },

  toJSON(message: GetFeedRequest): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    if (message.sourceId !== "") {
      obj.sourceId = message.sourceId;
    }
    if (message.subscriptionId !== "") {
      obj.subscriptionId = message.subscriptionId;
    }
    if (message.after !== undefined) {
      obj.after = message.after;
    }
    if (message.before !== undefined) {
      obj.before = message.before;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFeedRequest>, I>>(base?: I): GetFeedRequest {
    return GetFeedRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFeedRequest>, I>>(object: I): GetFeedRequest {
    const message = createBaseGetFeedRequest();
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    message.sourceId = object.sourceId ?? "";
    message.subscriptionId = object.subscriptionId ?? "";
    message.after = object.after ?? undefined;
    message.before = object.before ?? undefined;
    return message;
  },
};

function createBaseGetFeedResponse(): GetFeedResponse {
  return { items: [], page: undefined };
}

export const GetFeedResponse: MessageFns<GetFeedResponse> = {
  encode(message: GetFeedResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      FeedItem.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetFeedResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFeedResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(FeedItem.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFeedResponse {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => FeedItem.fromJSON(e)) : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: GetFeedResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => FeedItem.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFeedResponse>, I>>(base?: I): GetFeedResponse {
    return GetFeedResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFeedResponse>, I>>(object: I): GetFeedResponse {
    const message = createBaseGetFeedResponse();
    message.items = object.items?.map((e) => FeedItem.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    return message;
  },
};

/**
 * FeedService is the read-only surface for the authenticated user's feed.
 * All RPCs require a valid Firebase Auth token in gRPC metadata.
 */
export type FeedServiceDefinition = typeof FeedServiceDefinition;
export const FeedServiceDefinition = {
  name: "FeedService",
  fullName: "com.softmemes.myfeed.v1.FeedService",
  methods: {
    /**
     * GetFeed returns the user's feed in reverse-chronological order.
     * All filter fields are optional; omitting them returns the full feed.
     */
    getFeed: {
      name: "GetFeed",
      requestType: GetFeedRequest,
      requestStream: false,
      responseType: GetFeedResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface FeedServiceImplementation<CallContextExt = {}> {
  /**
   * GetFeed returns the user's feed in reverse-chronological order.
   * All filter fields are optional; omitting them returns the full feed.
   */
  getFeed(request: GetFeedRequest, context: CallContext & CallContextExt): Promise<DeepPartial<GetFeedResponse>>;
}

export interface FeedServiceClient<CallOptionsExt = {}> {
  /**
   * GetFeed returns the user's feed in reverse-chronological order.
   * All filter fields are optional; omitting them returns the full feed.
   */
  getFeed(request: DeepPartial<GetFeedRequest>, options?: CallOptions & CallOptionsExt): Promise<GetFeedResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
